<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd
		http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.3.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">

<bean id="bcryptPasswordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />
<bean id="customUserDetailsService" class="kr.co.hit.security.UserAuthenticationService"/>

		<!-- ##Security http## -->
		<security:http auto-config="true" use-expressions="true">
					<!-- 전체 접근 영역 -->
					<security:intercept-url pattern="/member/**" access="permitAll" />

					<!-- 정적 자원에 대한 시큐리티 해제 -->
					<security:intercept-url pattern="/resources/**" access="permitAll" />

					<!-- 로그인 사용자 이용 영역 -->
					<security:intercept-url pattern="/community/**" access="hasAnyRole('ROLE_USER', 'ROLE_ADMIN')"/>

					<!-- 로그인 -->					
					<security:csrf disabled="true"/>
					<security:form-login login-page="/member/login" username-parameter="member_id" password-parameter="password"/>
					
					
		</security:http>
		
		<!-- ##Security authentication## -->
		<security:authentication-manager>
			<security:authentication-provider user-service-ref="customUserDetailsService">
				<security:password-encoder ref="bcryptPasswordEncoder" />
			</security:authentication-provider>
		</security:authentication-manager>

</beans>
